# Test instance's localhost profile — same shape as the dev instance.
# Lives here so tests that resolve a profile via
# `<sidestage_dir>/llm_profiles/` find one when they need it.

models:
  default:
    endpoint: http://127.0.0.1:8080
    model: openai/local
